# Required for Python CLI + Supabase mail-sender
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
MAIL_SENDER_BEARER_TOKEN=

# Optional: defaults to ${SUPABASE_URL}/functions/v1/mail-sender
MAIL_SENDER_URL=

# Optional display name (currently mail-sender uses server-side from name)
FROM_NAME=Scott Crawford Coaching
